import React from 'react';
import Logo from '../SVGs/Logo';

import './Toolbar.css';

const Toolbar = () => {
  return (
    <div className='Toolbar'>
      <Logo/>
    </div>
  );
}

export default Toolbar;
